# FinQuant v0.2b — policy mismatch reinforcement

**Generated (UTC):** `2026-04-29T16:37:31Z`

## Purpose

Targeted patch for **policy vs implementation** drift. Adversarial rows teach **Incorrect** verdicts and **FAIL** status unless evidence proves YAML/registry/logs align. Clean rows teach audited alignment.

## Artifacts

| Metric | Value |
|--------|-------|
| Total records | 120 |
| Adversarial | 96 (80.0%) |
| Seed | 42004 |
| Flag | `policy_mismatch_v0.2b` |

## Coverage (mandatory themes)

| Theme | Present in adversarial templates |
|-------|----------------------------------|
| YAML/config vs execution drift | Yes |
| Declared ATR vs runtime / hard-coded multiple | Yes |
| Risk sizing vs ledger fixed size | Yes |
| Funding filter declared vs not applied | Yes |
| Indicator / dashboard bypass vs coded gate | Yes |
| Policy registry vs active runtime policy | Yes |
| Fee/slippage manifest vs ledger | Yes |
| Hardcoded defaults overriding YAML | Yes |
| Hidden fallback policy activation | Yes |
| Stale / wrong config file loaded | Yes |

## Ten sample records (abbrev.)

1. **Adv** — YAML `stop_loss_pct` vs engine using different threshold → Incorrect, FAIL.
2. **Adv** — TP from YAML vs hard-coded ATR multiple in logs → Incorrect — mismatch, FAIL.
3. **Adv** — Declared ATR stops vs fixed tick offsets → Incorrect, FAIL.
4. **Adv** — 1% equity sizing vs identical notional fills → Incorrect, FAIL.
5. **Adv** — Funding filter in YAML vs runtime accepts all entries → Incorrect, FAIL.
6. **Adv** — ADX gate vs dashboard hotlink bypass → Incorrect, FAIL.
7. **Adv** — Registry `momentum_v3` vs runner loading `mean_revert_v1` → Incorrect, FAIL.
8. **Adv** — Backtest manifest fee/slip vs ledger reconciliation → Incorrect, FAIL.
9. **Adv** — YAML max leverage 3x vs code default 10x → Incorrect, FAIL.
10. **Clean** — Auditor confirms YAML hash matches loader and fills reference same hash → Correct, PASS.

(Full rows: `finquant/patches/v0.2b/finquant_v0.2b_policy_mismatch_reinforcement.jsonl`.)

## Staging (trx40)

```text
/data/finquant-1/datasets/staging/finquant_v0.2b_policy_mismatch_reinforcement.jsonl
```

## Combined smoke training (v0.2 + v0.2b)

Merge with base v0.2 reinforcement **without** deduplicating lines:

```bash
export FINQUANT_BASE=/data/finquant-1
cd ~/blackbox
cat \
  /data/finquant-1/datasets/staging/finquant_v0.2_reinforcement.jsonl \
  /data/finquant-1/datasets/staging/finquant_v0.2b_policy_mismatch_reinforcement.jsonl \
  > /data/finquant-1/datasets/staging/finquant_v0.2_combined_tmp.jsonl
wc -l /data/finquant-1/datasets/staging/finquant_v0.2_combined_tmp.jsonl

python3 finquant/training/train_qlora.py smoke --config finquant/training/config_v0.1.yaml \
  --dataset /data/finquant-1/datasets/staging/finquant_v0.2_combined_tmp.jsonl \
  --output-dir /data/finquant-1/adapters/finquant-1-qwen7b-v0.2b-smoke

python3 finquant/evals/eval_finquant.py \
  --adapter /data/finquant-1/adapters/finquant-1-qwen7b-v0.2b-smoke \
  --write-report --report-path /data/finquant-1/reports/v0.2b_smoke_eval_report.md
```

## Acceptance

- `policy_mismatch` eval case **pass**
- `funding_liquidation` **still pass**
- Overall **6/6** on smoke eval harness

---

*Generator:* `finquant/scripts/generate_v0_2b_policy_mismatch_jsonl.py`*