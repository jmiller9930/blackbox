# RenaissanceV4 — Baseline v1 (locked)

**Promotion:** DV-ARCH-REVIEW-014 — **RenaissanceV4_baseline_v1**  
**Status:** LOCKED — do not modify signal logic, fusion math, risk thresholds, or stop/target geometry on this line; branch new work for experiments.

---

## Git pointer

| Item | Value |
|------|--------|
| **Annotated tag** | `RenaissanceV4_baseline_v1` |
| **Commit** | Resolve with `git rev-parse RenaissanceV4_baseline_v1` (includes this promotion record + artifacts below) |

**Code lineage:** Pipeline and reports match promotion stack through **`6b902564ac9fc3e827f56332e4ec579555c25ee8`**; the baseline tag may point to a doc-only follow-up that adds this file without changing strategy code.

After clone: `git fetch --tags && git checkout RenaissanceV4_baseline_v1`

---

## Validation metrics (accepted, full-history replay)

**Dataset:** ~210 240 five-minute bars (Binance SOLUSDT-style ingest; same DB as engineering validation on **clawbot**).  
**Source for post-correction tables:** `diagnostic_quality_post_DV013.md` (generated by `python3 -m renaissance_v4.research.diagnostic_quality_pipeline` at baseline commit).

| Metric | Value |
|--------|--------|
| Closed trades | 1969 |
| Win rate | 0.3443 |
| Expectancy | +0.008256 |
| Max drawdown (path) | 24.297305 |
| Total gross PnL | +16.256692 |
| Target share of exits | ~34% (678 / 1969) |
| `full` tier trades | 0 |

**Residual (acceptable for baseline, not blockers):** exit-time `unstable` still negative in isolation; stop exits still outnumber targets; mean reversion remains the main volume driver; no full-tier sizing by design.

---

## Canonical report artifacts (frozen at tag)

These files form the audit trail; do not overwrite in place — use new filenames or branches for new runs.

| Artifact | Role |
|----------|------|
| `renaissance_v4/reports/correction_v1.md` | Fusion correction (DV-ARCH-CORRECTION-009) |
| `renaissance_v4/reports/correction_risk_v1.md` | Risk calibration (pre–economic-quality) |
| `renaissance_v4/reports/diagnostic_quality_v1.md` | Economic quality diagnostic **before** DV-013 (1702 trades, negative E) |
| `renaissance_v4/reports/correction_quality_v1.md` | Economic correction DV-ARCH-CORRECTION-013 |
| `renaissance_v4/reports/diagnostic_quality_post_DV013.md` | Full replay metrics **after** DV-013 (promotion evidence) |
| `renaissance_v4/reports/baseline_v1.md` | This file — promotion record and lock rule |

---

## Baseline lock (engineering)

Until superseded by a **new** promoted baseline tag:

- No changes to **signal** modules’ decision rules.
- No changes to **fusion** scoring math or thresholds (`fusion_engine.py` contract).
- No changes to **risk** tier thresholds or governor policy (`risk_governor.py` contract).
- No changes to **ATR stop/target** multiples (`execution_manager.py` contract).

Robustness, Monte Carlo, sensitivity, and “what-if” work happen on a **separate branch** and compare against this tag.

---

## Next phase branch

Authorized work: **Robustness & edge validation** (Monte Carlo, regime stability, stress tests, drawdown distributions, parameter sensitivity).

Default integration branch name (empty tip = baseline, or first commit from tag): **`robustness-edge-validation`** — create from `RenaissanceV4_baseline_v1` when starting experiments.
