{
  "schema": "reasoning_router_secrets_local_v1",
  "contract_version": 1,
  "_note": "``OPENAI_API_KEY`` in the process environment wins, else the **single** host file in ``external_openai_secrets_contract_v1.py`` (default ``~/.blackbox_secrets/openai.env``; override ``BLACKBOX_OPENAI_ENV_FILE``). No other OpenAI key store in this repo. This JSON is not read by the router."
}
