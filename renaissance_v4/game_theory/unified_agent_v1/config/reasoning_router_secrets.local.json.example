{
  "schema": "reasoning_router_secrets_local_v1",
  "contract_version": 1,
  "_note": "GT_DIRECTIVE_026AI lab OpenAI proof: do not use a secrets file. Set OPENAI_API_KEY only in the process environment (e.g. ``export OPENAI_API_KEY=...`` on clawbot). This JSON shape is not read by the adapter; kept for local documentation only."
}
