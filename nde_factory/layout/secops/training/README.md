# SecOps — training scripts (`/data/NDE/secops/training`)

Domain-local generators and training configs live here.

## Planned

| Script | Purpose |
|--------|---------|
| `cmmc_source_to_training.py` | First generator: CMMC-oriented paths from extracted sources → staging JSONL ( **not implemented** until approved ). |

No training subprocess is started by files in this folder until operators explicitly run approved tooling.
